{
  "name": "@shapeshiftoss/mastra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "rm -rf dist && yarn run -T tsc --build",
    "dev": "yarn run -T tsc --build --watch",
    "type-check": "yarn run -T tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/openai": "^1.3.22",
    "@mastra/core": "^0.11.1",
    "@mastra/libsql": "^0.11.2",
    "@mastra/loggers": "^0.10.4",
    "@mastra/memory": "^0.11.5",
    "zod": "*"
  }
}
