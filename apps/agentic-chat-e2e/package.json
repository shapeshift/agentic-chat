{
  "name": "@shapeshiftoss/agentic-chat-e2e",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf test-results",
    "test": "playwright test",
    "type-check": "yarn run -T tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.36.0"
  }
}
